[project]
name = "bms-ticket-notifier"
version = "0.1.0"
description = "Notify me when BookMyShow tickets change (dates open / seats free up)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
]
